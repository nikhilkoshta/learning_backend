function userMiddleware(req, res, next) {
    //Implement auth logic
    //Need to check the headers and validate the user from the user DB. Check reqadme for the exact headers to be expected

}

module.exports = userMiddleware;